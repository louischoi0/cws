# KDS Storage Engine — Overview

See `/CLAUDE.md` at the repo root for the architecture summary and the agent working guide, and `docs/spec/heap-and-tuple.md` for the authoritative design specification. This file exists only so that references to it resolve.
